export class Ingredient {
    constructor(public name: string, public amount: number) {
    }
}

// this create two properties name and amount in the class and stores the arguments of the constructor against 
// these variables when instanciated..
